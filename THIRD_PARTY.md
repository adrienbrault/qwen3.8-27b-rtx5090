# Third-party code and attribution

This repository is MIT-licensed (see [LICENSE](LICENSE)) for the **original** work: documentation, benchmark harnesses/probes, serve scripts, and the patch-installer tooling. Everything below is derived from Apache-2.0 projects and **remains Apache-2.0-derived** — the MIT grant does not extend to upstream-derived content, and the patched files inside any image you build are derivative works of their upstream projects.

## Derived from vLLM (Apache-2.0)

Upstream: https://github.com/vllm-project/vllm — [Apache-2.0](https://github.com/vllm-project/vllm/blob/main/LICENSE).

| file | derivation |
|---|---|
| `patches/vllm-only.diff` | **Verbatim redistribution** of the vLLM-source portion of [vllm#40914](https://github.com/vllm-project/vllm/pull/40914) (TurboQuant K+1 spec-verify routing), authored by **@Sandermage**, contributed to vLLM under Apache-2.0. Redistributed unmodified so the setup is reproducible while the PR is unmerged. |
| `patches/so_reasoning_44993.diff` | **Redistribution** of the diff from [vllm#44993](https://github.com/vllm-project/vllm/pull/44993) (structured-output grammar across reasoning boundary), authored by **@yuyue0225sc**, contributed to vLLM under Apache-2.0. PR open at time of writing. |
| `patches/install_pr42603_sync.py` | Original installer (MIT), but the one-line change it grafts is **from [vllm#42603](https://github.com/vllm-project/vllm/pull/42603)** (closed unmerged) and the anchor snippets it matches are quoted vLLM source. The grafted `llm_base_proposer.py` in the image is Apache-2.0-derived. |
| `patches/fix_spec_output.py`, `patches/fix_spec_guard.py`, `patches/tq_auto_fallback.py`, `patches/tq_splits.py` | Original installers (MIT) that modify vLLM source files at build time and quote short anchor snippets to locate edit sites. The resulting patched files are Apache-2.0-derived. |
| `patches/lmcache/0003-vllm-connector-eagle-hybrid-hit.diff`, `patches/lmcache/0005-vllm-residual-mamba-connector-prefill-boundary.diff` | Original fixes (offered upstream under Apache-2.0), expressed as diffs **against vLLM source** — the context lines are vLLM code and the patched files are Apache-2.0-derived. |
| `patches/lmcache-0.5.1-format10-NL_X_NB_NH_BS_TWO_HS.patch` | Withdrawn historical patch (see `patches/README.md`); diff against LMCache source, Apache-2.0-derived. |

Files modified in the image by the entries above include: `vllm/v1/spec_decode/llm_base_proposer.py`, `vllm/v1/structured_output/__init__.py`, `vllm/v1/core/sched/scheduler.py`, `vllm/v1/worker/gpu_model_runner.py`, `vllm/v1/attention/backends/turboquant_attn.py`, `vllm/model_executor/layers/quantization/turboquant/config.py`, `vllm/config/attention.py`.

## Derived from LMCache (Apache-2.0)

Upstream: https://github.com/LMCache/LMCache — [Apache-2.0](https://github.com/LMCache/LMCache/blob/dev/LICENSE).

| file | derivation |
|---|---|
| `patches/lmcache/0001-fix-fused-hybrid-subpage-view.diff`, `0002-strided-fp8-regroup.diff`, `0007-sidecar-vram-staging-batch.diff`, `0008-fs-native-cap-enforcement.diff` | Original fixes (offered upstream under Apache-2.0), expressed as diffs against LMCache source (Python and `csrc`) — context lines are LMCache code and the patched build is Apache-2.0-derived. |

### Patch stacks added after 2026-07-22

| files | provenance |
|---|---|
| `patches/rc4/` (8 diffs + `rc4-stack.diff`) | Original fixes and the combined tier-rc4 stack, expressed as diffs against LMCache 0.5.4rc4 and vLLM source — context lines are Apache-2.0 code; offered upstream under Apache-2.0. |
| `patches-nvfp4kv/` | `upstream-pr49891-original.diff` is a verbatim redistribution of ch2lab's vLLM PR [#49891](https://github.com/vllm-project/vllm/pull/49891) (Apache-2.0, credited in the directory README's lineage section); the remaining diffs and the `overlay/` package are rebases/derivatives of that PR and of vLLM/FlashInfer code, Apache-2.0. |
| `patches-dflash2/` | Derived from vLLM PR [#52816](https://github.com/vllm-project/vllm/pull/52816) and syv-ai's backport (Apache-2.0, linked in the directory README). |
| `patches-v0280/` (the v0.28.0 daily generation, 2026-08-28) | `0101` is a re-rebase of **ch2lab's** vLLM PR [#49891](https://github.com/vllm-project/vllm/pull/49891) (sm12x NVFP4→FA2 routing) onto v0.28.0. `0102` carries **drowzeys'** linear-V-scale writer fix (first shipped in their [DGX Spark repo](https://github.com/drowzeys/keys-vLLm.0.27-Qwen3.8-27B-ADay777Ablit-NVFP4-A4Q-NVFP4-KV-4M-KV-token-pool-MTP3-Single-DGX-Spark)). `0103` (XQA-NVFP4 decode) implements the architecture demonstrated by the author of vLLM issue [#49011](https://github.com/vllm-project/vllm/issues/49011) (XQA decode + FA2 prefill on sm120); **hikarioyama's** [vllm-nvfp4-kv-sm120](https://github.com/hikarioyama/vllm-nvfp4-kv-sm120) (FA2 explicit-SF-stride) was prior art consulted for the gate/kernel mechanics. `0104` re-rebases the drafter FULL-cudagraph routing from PR #49891. The in-progress DFlash2-draft-on-NVFP4 route (`0105`) ports the non-causal `backend="fa2"` wrapper approach from **seanyourhighness's** [vllm-sm12x-nvfp4-dflash2](https://github.com/seanyourhighness/vllm-sm12x-nvfp4-dflash2) overlay (Apache-2.0), whose repo also validated the `--kv-cache-memory-bytes` pool pinning we use and whose DFlash2 selector-walk sampling rewrite informs our acceptance investigation. The dropped-async-flag doctrine follows **Ronald1995's** vLLM PR [#24799](https://github.com/vllm-project/vllm/pull/24799) (async scheduling × spec decode). All diffs are against Apache-2.0 vLLM/FlashInfer source; the patched images are Apache-2.0-derived. |

## Base images

- `vllm/vllm-openai:v0.28.0` (the current daily generation's base, `patches-v0280/Dockerfile.v0280-nvfp4kv`) — Apache-2.0.
- `vllm/vllm-openai:nightly` (pinned by digest in `patches/Dockerfile`, earlier generations) — Apache-2.0. This repo ships no vLLM binaries; the Dockerfiles pull the official image and patch it locally.
- `lmcache/vllm-openai:*` (historical, docs only) — Apache-2.0.

## Models (not redistributed here)

- [`RedHatAI/Qwen3.8-27B-NVFP4`](https://huggingface.co/RedHatAI/Qwen3.8-27B-NVFP4) — **the current daily's weights (since 2026-09-02)**: llm-compressor NVFP4 with 303 modules kept at 8-bit and an FP8 `lm_head`; served with its own chat template. Qwen3.8 base license. Chosen by the R156 fidelity ladder (bench/RESULTS.md); [`unsloth/Qwen3.8-27B-NVFP4`](https://huggingface.co/unsloth/Qwen3.8-27B-NVFP4) and [`kelnei/Qwen3.8-27B-NVFP4`](https://huggingface.co/kelnei/Qwen3.8-27B-NVFP4) tie it on fidelity (same recipe family) and were the other two candidates.
- [`gittensor-model-hub/Qwen3.8-27B-NVFP4-RTX5090-LMHead4`](https://huggingface.co/gittensor-model-hub/Qwen3.8-27B-NVFP4-RTX5090-LMHead4) — the 2026-08-22→09-02 daily (see the 2026-08-22 row of docs/HISTORY.md: bit-identical to our 2026-08-22 download of the parent repo); every 2026-08 number in this repo was measured on it. Qwen3.8 base license.
- [`Qwen/Qwen3.8-27B`](https://huggingface.co/Qwen/Qwen3.8-27B) (bf16) — the ground truth for the fidelity ladder; [`RadixArk`](https://huggingface.co/RadixArk), [`QUASAR`](https://huggingface.co/QUASAR) QAT and the fp8 reference were the other rungs.
- [`syv-ai/DFlash2-Qwen3.8-27B-W4A16`](https://huggingface.co/syv-ai) — the DFlash2 draft used in speculative-decoding experiments; [`incoai/Qwen3.8-27B-DFlash2`](https://huggingface.co/incoai/Qwen3.8-27B-DFlash2) is the original bf16 draft.
- [`sakamakismile/Qwen3.8-27B-MTP-NVFP4`](https://huggingface.co/sakamakismile/Qwen3.8-27B-MTP-NVFP4) — the 2026-08-14→22 daily; its Qwen3.8 XML chat template is what the gittensor daily served with.
- [`natfii/Qwen3.6-27B-VLM-NVFP4-MTP`](https://huggingface.co/natfii/Qwen3.6-27B-VLM-NVFP4-MTP) — the Qwen3.6-era daily (validated snapshot revision `2e46c0ed7606f35e357bc5674d20c710fc51b178`). Qwen3.6 base license (Apache-2.0).
- [`Lorbus/Qwen3.6-27B-int4-AutoRound`](https://huggingface.co/Lorbus/Qwen3.6-27B-int4-AutoRound) — an earlier (Qwen3.6-era) daily, still the W4A16 reference.
- [`unsloth/Qwen3.6-27B-NVFP4`](https://huggingface.co/unsloth/Qwen3.6-27B-NVFP4) — earlier daily (TurboQuant era).

## Benchmarks and tools (not redistributed here)

- [Terminal-Bench / Harbor](https://www.tbench.ai/) — Apache-2.0.
- [SWE-bench](https://github.com/SWE-bench/SWE-bench) and [R2E-Gym](https://github.com/R2E-Gym/R2E-Gym).
- [llama-benchy](https://github.com/eugr/llama-benchy).
- [tool-eval-bench](https://github.com/SeraphimSerapis/tool-eval-bench).
- [aider](https://github.com/Aider-AI/aider) — Apache-2.0.

## GPU driver P2P (dual-5090 host config)

The serving host runs [QuixiAI/open-gpu-kernel-modules](https://github.com/QuixiAI/open-gpu-kernel-modules) (Eric Hartford) — NVIDIA's open GPU kernel modules (dual MIT/GPL) with PCIe peer-to-peer force-enabled for consumer GeForce cards. The technique originates with [tinygrad / George Hotz's P2P patch](https://github.com/tinygrad/open-gpu-kernel-modules), later simplified by **aikitoria**, and ported to the 610 driver series with Blackwell support by QuixiAI. `scripts/gpu-p2p-610.sh` (ours, MIT) automates their install on Ubuntu, and the `RMForceStaticBar1=1` + `NVreg_EnableResizableBar=1` regkey pair plus the `iommu=pt` requirement come from that fork's documentation and our validation on a 2x RTX 5090 / X870 box.

## If you upstream this

The fixes in `fix_spec_output.py` and the six `patches/lmcache/` diffs belong in vLLM/LMCache, not in a patch repo. They are offered to those projects under Apache-2.0, on the same terms as any other contribution.

## Ported open PRs (2026-09-03, `patches-v0290/`, verdicts in bench/RESULTS.md R165b/c)

- **[vllm#53543](https://github.com/vllm-project/vllm/pull/53543)** (seanyourhighness) — masked NVFP4 XQA speculative verification on SM120, model-dtype queries/outputs, opt-in isolated CUDA stream. Ported as `0132-masked-nvfp4-xqa-sm120-v0290.diff` onto the rebased chain. Verdict (R165c): correct on Qwen3.8-27B/SM120 (needles pass through XQA verification, the earlier wrong-verify failure is gone) but 24–25% slower than the FA2-NVFP4 route at c1/c8 on this geometry; not adopted. The first port dropped the PR's assertion removal; that was our error, not the PR's.
- **[vllm#54181](https://github.com/vllm-project/vllm/pull/54181)** (F21HGG) — SM120 packed GDN decode launch `BV=16` for batches ≤ 24. Ported as `0133-gdn-packed-decode-bv16-v0290.diff` with a local `VLLM_SM12X_GDN_PACKED_BV` override (the PR's selector requires 16 value heads; this model has 48). Verdict (R165b): forced BV=16 on this geometry is inconclusive at c1 (inside the box's single-stream bimodality) and −6% at c8 decode on two runs; not adopted here, which says nothing about the PR's intended H=HV=16 geometry.
- **[vllm#54163](https://github.com/vllm-project/vllm/pull/54163)** (Ledgero) — DFlash/DSpark drafters draft from their own KV cache and never write target blocks, so the KV cache manager must not apply eagle block handling to them. `0134-dflash-no-eagle-block-drop-v0290.diff` is an rc1-native port of the predicate (the PR targets a newer main); without it v0.29.0rc1 + Qwen3.8-27B + DFlash2 serves zero prefix-cache hits and the disk tier stores without ever serving (bench/RESULTS.md R165). Verdict (R165b): proven — warm revisit 0.166 s / 53,248 hits with 0134 vs 7.5 s / 0 hits without. The root-cause analysis in that PR and in the closed [vllm#54041](https://github.com/vllm-project/vllm/pull/54041) (positive666) is what pointed at the mechanism.
- The v0.29.0rc1 rebase of the whole chain (`patches-v0290/`, NOTES16.md) was produced with codex (gpt-5.6-sol) from local source dumps; every build and measurement runs on the host by the maintainer.

- **[vllm#53981](https://github.com/vllm-project/vllm/pull/53981)** (waizuichougou) — routes directly constructed `VocabParallelEmbedding` modules through vLLM's existing UVA CPU offloader, so `--offload-backend uva --cpu-offload-gb N --cpu-offload-params embed_tokens` reaches the input embedding table (built outside `make_layers`, where the selector silently matched nothing). Fixes [vllm#53931](https://github.com/vllm-project/vllm/issues/53931) (webcodes-cz), which was filed with exactly this model as the repro. Ported verbatim minus its tests hunk as `0135-embed-uva-offload-v0290.diff` (applies clean to rc1; does not apply to v0.28.0). The idea of keeping the 2.37 GiB BF16 table in pinned host RAM and gathering rows over PCIe was brought to our attention by [sglang#37826](https://github.com/sgl-project/sglang/pull/37826) (nbald), which does the same with a Triton host gather; in vLLM the plain `F.embedding` on the UVA view plays that role. Source review of the port for our TP=2 / DFlash2 / pinned-pool shape: `patches-v0290/NOTES18.md` (codex, gpt-5.6-sol). Verdict (R167, 2026-09-03): validated on the rc1 chain — +9.3% KV pool with nvfp4 KV (+82,811 tokens, the whole 1.18 GiB shard) and +8.4% with fp8, no measurable decode or fidelity cost at c1/c8/30K context, needles clean. Not on the daily yet: the daily runs v0.28, where the patch does not apply, and the rc1 nvfp4 route had a deep-context decode regression — attributed the same evening to FlashInfer 0.6.18's NVFP4 split-KV guard (next entry).

- **FlashInfer 0.6.18 `_nvfp4_kv_requires_disabled_split_kv`** ([flashinfer-ai/flashinfer](https://github.com/flashinfer-ai/flashinfer), `flashinfer/prefill.py`, first shipped in the 0.6.17/0.6.18 window) — the FA2 paged-prefill wrapper overrides the caller's `disable_split_kv=False` for packed-NVFP4 KV, as an empirical workaround for corrupted outputs when a short query attends a long KV range. On our route (speculative verification = 10 query tokens → prefill wrapper) it turns 30K-context decode from 144 to 29 tok/s (R167/R168). `0136-opt-in-nvfp4-prefill-split-kv-v0290.diff` (codex, gpt-5.6-sol, from our BRIEF19; source analysis in `patches-v0290/NOTES19.md`) is an opt-in knob `VLLM_SM12X_NVFP4_PREFILL_SPLIT_KV=1` that restores the 0.6.16.post3 behaviour on the SM12x NVFP4 route only; off by default because the corruption FlashInfer saw is the family our Bug B dodge exists for. Verdict (r168b, 2026-09-03): with the knob on, rc2 decodes 141.9 tok/s at 30K context (27.4 off; 143 on v0.28), short-context speed unchanged, needles 12/12 to 220K, zero error lines, and the teacher-forced ruler is bit-identical on vs off — because prompt-logprob scoring is a long-query prefill that FlashInfer never splits, i.e. that ruler cannot see the knob at all. The decode-path adjudication (T=0 generations at 30K and a cached-prefix depth ladder, on vs off) is r168c.
- **`0137-fs-tier-eviction-v0290.diff` — LRU eviction inside vLLM's filesystem KV tier** (codex, gpt-5.6-sol, from our BRIEF20; design notes `patches-v0290/NOTES20.md`, tests in the patch, `verify-0137.sh`). vLLM's `FileSystemTierManager` (the `TieringOffloadingSpec` fs secondary tier, 0.28 and 0.29) writes forever and never deletes: the tier fills the filesystem, stores fail with ENOSPC (logged, engine alive) and a restart cannot come back. The patch adds `max_capacity_gb` / `evict_high_ratio` / `evict_low_ratio` / `min_free_gb` / `evict_scope` to the tier config, a startup scan, LRU on lookup/load/store, and pins for files a lookup, a hit request or a transfer job references (the offloading worker asserts on a failed load, so an external sweeper is unsafe under load). Applies `--fuzz=0` to v0.28.0, rc1 and rc2. Verdict: pending r170 (flood under a 24 GB cap on both the rc2 candidate and the v0.28 daily image).

## Upstream work audited 2026-08-31 (verdicts in bench/RESULTS.md; credited regardless of adoption)

- **[vllm#53479](https://github.com/vllm-project/vllm/pull/53479)** (mamba-align boundary states) and **[vllm#53670](https://github.com/vllm-project/vllm/issues/53670)** (EAGLE last-block drop cost on hybrid GDN) — the prefix-cache line we backported and measured; the issue's warm-revisit methodology inspired our `probes/warm-revisit` probe.
- **[vllm#53426](https://github.com/vllm-project/vllm/pull/53426)** + **[vllm#51575](https://github.com/vllm-project/vllm/pull/51575)** — the `skip_draft_when_k0` mechanism and its runtime-K propagation; our 0115 backport reproduces both on v0.28.0.
- **[maurienne-ai/Qwen3.8-27B-DFlash2-NVFP4-RTNcal](https://huggingface.co/maurienne-ai/Qwen3.8-27B-DFlash2-NVFP4-RTNcal)** — calibrated W4A4 DFlash2 drafter; auditioning it exposed and fixed a real bug in our quantized-draft loader patch (0114).
- **[seanyourhighness/vllm-sm12x-nvfp4-dflash2](https://github.com/seanyourhighness/vllm-sm12x-nvfp4-dflash2)** — pinned all-NVFP4 DFlash2 overlay for sm12x on vLLM v0.27.1; independent evidence for the DFlash2-on-NVFP4 route and source of the eager-drafter/XQA-interference note. Verdict (R166, 2026-09-03): the `--kv-cache-memory-bytes` pool pinning that repo validated is now how our nvfp4 candidate launcher sizes its pool (per sequence count, with a post-pre-warm free-memory guard); it is what makes the route boot with headroom on v0.28.
- **[vllm#53979](https://github.com/vllm-project/vllm/pull/53979)/[#53978](https://github.com/vllm-project/vllm/pull/53978)/[#53977](https://github.com/vllm-project/vllm/pull/53977)** — the upstream FA2 non-causal NVFP4 route, now built and load-bearing here (patches 0116/0117; see bench/RESULTS.md R155).
- **0129 / R158** (own work, 2026-09-02): drafter CUDA graphs on the sm120 NVFP4 route. The eager-drafter precaution it removes traces to the XQA-interference note in seanyourhighness's overlay above; the profiler A/B and the 576-cell FA2-NVFP4 differential harness (`scripts/nvfp4_fa2_harness.py`) are ours, against Apache-2.0 vLLM/FlashInfer source.
- **[flashinfer PR #4393](https://github.com/flashinfer-ai/flashinfer/pull/4393), the `pcie_ipc` all-reduce** ([flashinfer-ai/flashinfer](https://github.com/flashinfer-ai/flashinfer) main, merged 2026-08-20, measured at commit df8b5c1 of 2026-09-04; not in any 0.6.x release) — an all-reduce for NVLink-less multi-GPU boxes, with its autotuner and the one-workspace-one-stream contract. Used unmodified by `scripts/ar_bench.py` (R184) through a main checkout mounted over the package; the comparison arms are vLLM's `CustomAllreduce` and NCCL via `torch.distributed`, also unmodified. Not in the served image. The row-count table, the graph-replay correctness check and the per-rank group-max timing convention (taken from FlashInfer's own `benchmarks/bench_pcie_ipc_all_reduce.py`) are ours.
- **`0138-pcie-ipc-all-reduce-v0290.diff` + `patches-v0290/pcie_ipc_ar21/` — FlashInfer main's `pcie_ipc` all-reduce as an opt-in TP=2 all-reduce inside vLLM** (codex, gpt-6-astra, from our BRIEF21; design notes `patches-v0290/NOTES21.md`, `verify-0138.sh`, `Dockerfile.pcieipc`). `pcie_ipc_ar21/include/flashinfer/comm/pcie_ipc_all_reduce.cuh` is FlashInfer main commit df8b5c1 (2026-09-04) copied unmodified, Apache-2.0, see the package `NOTICE`. `csrc/bindings.cu` is a PyTorch/CUDA ownership adapter that replaces main's TVM FFI binding; `workspace.py` replaces main's Python ownership and preparation glue (IPC slab exchange over gloo, shape preparation before graph capture, stream rebinding); `fixed_config.py` carries the eight TP2 launch triples of `pcie-tune-free.json`, the R184 autotune output kept byte-for-byte, which were tuned at an 8,192-row workspace and are reused here at a 320-row one — a stated assumption, not a measurement. The vLLM side adds a `pcie_ipc_all_reduce.py` device communicator ahead of `CustomAllreduce` under `VLLM_SM12X_PCIE_IPC_AR=1` (bf16 2-D inputs up to 320 rows; anything else falls through to the existing chain). Zero bytes of FlashInfer's `jit/core.py` or autotuner are copied; FlashInfer 0.6.16.post3 stays installed and untouched. Not in the served image: built as `...-fi0616-pcieipc` by `Dockerfile.pcieipc`. `scripts/ar_bench.py` gained a `vend` backend that times the vendored package next to the main checkout it was cut from (2026-09-04 22:45 UTC, `results/2026-09-05-r185-pcieipc/arbench-gate.out`: graph-replay error 0 against NCCL, within 0.5 µs of main at every row from 1 to 320). Verdict (R185 + R185b, 2026-09-04, `results/2026-09-05-r185-pcieipc`): the engine arms ran; knob on vs the same image with the knob off reads +4.6% code and +5.4% prose single-stream decode, +3.1% prose at 30K, +2.6% at 16 streams, and a flat 8-stream tokens/s that is +4.9% in step rate; numerics identical to the digit on every paired ruler. Promoted 2026-09-05: served by `scripts/serve-r168-daily.sh` on the `...-fi0616-pcieipc` image with `VLLM_SM12X_PCIE_IPC_AR=1` asserted at boot; the live-port gates are `scripts/r189-promote-pcieipc.sh` (R189).
- **`0139-nvfp4-marlin-allowlist-v0290.diff` — per-layer NVFP4 GEMM kernel selection (`VLLM_SM12X_NVFP4_MARLIN_LAYERS`)** (codex, gpt-6-astra, from our `patches-v0290/BRIEF22.md`; design notes `patches-v0290/NOTES22.md`, `verify-0139.sh`, `Dockerfile.marlinlist`). 138 diff lines against the served rc2 tree: a stdlib-only helper `vllm/nvfp4_marlin_allowlist.py` (regex parse + match), an `envs.py` entry, `layer_name` threaded from the compressed-tensors W4A4 scheme construction into `init_nvfp4_linear_kernel`, which instantiates `MarlinNvFp4LinearKernel` for a matched module before the normal selection ladder and raises on any conflict (`--linear-backend` other than auto, Marlin in `VLLM_DISABLED_KERNELS`, `VLLM_BATCH_INVARIANT`). Unset knob = the original selection, no new log lines. The motivating measurement is R183b (2026-09-04): Marlin on every NVFP4 layer is +0.207% PPL vs bf16 against FlashInferCutlass's +0.744%, at −7.8% c8 / −17.3% c16 decode. Verified offline only (scratch-tree apply at fuzz 0, py_compile, matcher tests); the image `...-fi0616-marlinlist` was built 2026-09-04 23:35 UTC with the patch applied at fuzz 0. Not served; the R188 arms (`scripts/r188-marlin-allowlist.sh`) are queued.
- **R190 codex deliverables (2026-09-05, all codex gpt-6-astra from `patches-v0290/BRIEF-R190-COMMON.md` + `BRIEF23.md`..`BRIEF28.md`; design notes `NOTES23.md`..`NOTES28.md`; each verified offline only: scratch-tree apply at fuzz 0, py_compile, dependency-free tests, then a real apply at fuzz 0 inside the served image; nothing served, no GPU number yet).** All new Python is original Apache-2.0 glue against the served vLLM 0.29.0rc2 tree (patches 0101..0138 applied); the exact upstream vLLM commit of the rc2 tree is not identified and is not claimed. Follow-up 2026-09-05 (BRIEF25b): `0142c` replaces `0142` with thread-owned census scopes so the census survives async scheduling.
  - `0140-nvfp4-shape-dispatch-v0290.diff` (applies on top of 0139) / `0140a-...` (standalone) — `VLLM_SM12X_NVFP4_DISPATCH_TABLE=<json>`: per-module, per-M-bucket NVFP4 GEMM kernel selection among the four kernel classes that share one packed-weight layout (`CutlassNvFp4`, `FlashInferCutlass`, `FlashInferB12x`, `FlashInferCudnn`), no second weight copy; plus `r190/gemm/nvfp4_gemm_census.py` (times every NVFP4 and FP8 kernel class at the per-rank served shapes, M 1..8192), `build_shape_census.py` / `gemm_shape_census.md` (static per-module shape census: fused QKV is N=7168, fused GDN QKVZ is N=8192 per rank) and `make_dispatch_table.py` (only >5% winners become rules). The kernel implementations are vLLM's adapters over FlashInfer 0.6.16 / CUTLASS / Marlin / Humming / FBGEMM (their own notices in the image).
  - `0141-gdn-decode-tuning-v0290.diff` — `VLLM_SM12X_GDN_DECODE_CFG`: launch-config grammar and a head-fast split-grid variant of the packed GDN decode kernel (flash-linear-attention, MIT, Songlin Yang and Yu Zhang, vendored by vLLM under `vllm/third_party/`; base launch config from our 0133). Codex found this kernel serves only spec-OFF decode; under the served ns9 it is never called, so 0141 is a spec-off tuning only.
  - `0145-gdn-spec-update-tuning-v0290.diff` — `VLLM_SM12X_GDN_SPEC_CFG`: the same grammar for `fused_sigmoid_gating_delta_rule_update_kernel` (same flash-linear-attention provenance), the kernel the ns9 step actually runs 48 times per step (R183 profiles: 10 us per call at c1, 55 us at c16); tiled BV/warps/stages variants and a head-fast split grid, arithmetic unchanged; `r190/gdn/gdn_spec_microbench.py`.
  - `0142-dflash-sync-census-v0290.diff` — `VLLM_SM12X_SYNC_CENSUS=1`: per-step census of host-synchronising calls and graph replays by vLLM call site (torch sync-debug mode plus explicit hooks). `0142b-dflash-device-side-accept-v0290.diff` is an audit gate only: codex found the served V2 runner already keeps acceptance counts, positions and selector indices on device, so its proof line truthfully reports 0 syncs removed.
  - `0143-vocab-collective-tags-v0290.diff` — `VLLM_SM12X_COLLECTIVE_TAGS=1`: NVTX / record_function tags on every vocabulary-sized collective plus `r190/vocab/prof-vocab-0143.py`. The audit in NOTES26 found one full target logits all-gather per step (80x124160 bf16 at c8) and two 16-column DFlash2 candidate gathers, and that `--enable-batch-sharded-sampling` composes with DFlash verification (one all-to-all, half the bytes).
  - `0144-pcie-ipc-fused-norm-v0290.diff` — `VLLM_SM12X_PCIE_IPC_AR_FUSED_NORM=1`: the all-reduce output consumed in-register by residual add + Gemma RMSNorm, routed through vLLM's `AllReduceFusedAddGemmaRMSNormPattern` compile pass. New `pcie_ipc_ar21/csrc/pcie_ipc_fused_norm.cu` is a **derivative work of FlashInfer's `ipc_tp2_remote_push_kernel`** (FlashInfer main df8b5c1745c51f44b1e8a492b11c3f6cb157cfd2, Copyright (c) 2026 FlashInfer team, Apache-2.0): it reuses that kernel's scratch-epoch, volatile publish/poll and packed bf16 add helpers from the vendored header, which stays unmodified. One nvcc fix by us (bf16 to float conversion of the norm weight); the extension builds (image `...-pcieipc-fusednorm`, 2026-09-05). Probe `r190/pcie/pcie_fused_norm_check.py`.
  - `r190/drafter/` — corpus builder, TP1 eager target hidden-state capture at layers [5,19,33,47,61] through `LLM.apply_model` hooks, and a Prometheus-based acceptance meter (metric names from vLLM `v1/spec_decode/metrics.py`). The W4A16 re-quantisation stage is NOT implemented (no llm-compressor API evidence offline; the script exits 2 there). `drafter-reference.json` is the served syvai DFlash2 drafter config (its upstream licence is the drafter's own).

