--- a/vllm/envs.py
+++ b/vllm/envs.py
@@ -127,6 +127,7 @@
     VLLM_DISABLED_KERNELS: list[str] = []
     VLLM_USE_HW_AGNOSTIC: bool = False
     VLLM_ENABLE_FLA_PACKED_RECURRENT_DECODE: bool = True
+    VLLM_SM12X_GDN_SPEC_CFG: str | None = None
     VLLM_SM12X_GDN_PACKED_BV: int | None = None
     VLLM_GDN_DECODE_KERNEL: Literal["cuda", "triton"] = "cuda"
     VLLM_DISABLE_PYNCCL: bool = False
@@ -1205,6 +1206,7 @@
     "VLLM_ENABLE_FLA_PACKED_RECURRENT_DECODE": lambda: bool(
         int(os.getenv("VLLM_ENABLE_FLA_PACKED_RECURRENT_DECODE", "1"))
     ),
+    "VLLM_SM12X_GDN_SPEC_CFG": lambda: os.getenv("VLLM_SM12X_GDN_SPEC_CFG"),
     "VLLM_SM12X_GDN_PACKED_BV": lambda: (
         None
         if "VLLM_SM12X_GDN_PACKED_BV" not in os.environ
--- a/vllm/third_party/flash_linear_attention/ops/fused_sigmoid_gating.py
+++ b/vllm/third_party/flash_linear_attention/ops/fused_sigmoid_gating.py
@@ -9,7 +9,14 @@
 
 import torch
 
+from vllm import envs
+from vllm.logger import init_logger
+from vllm.platforms import current_platform
 from vllm.triton_utils import tl, triton
+
+from .gdn_spec_config import parse_spec_config
+
+logger = init_logger(__name__)
 
 
 @triton.heuristics(
@@ -182,6 +189,180 @@
         p_a += HV
 
 
+@triton.heuristics(
+    {
+        "USE_INITIAL_STATE": lambda args: args["h0"] is not None,
+        "IS_VARLEN": lambda args: args["cu_seqlens"] is not None,
+        "IS_CONTINUOUS_BATCHING": lambda args: args["ssm_state_indices"] is not None,
+        "IS_SPEC_DECODING": lambda args: args["num_accepted_tokens"] is not None,
+    }
+)
+@triton.jit(do_not_specialize=["N", "T"])
+def fused_sigmoid_gating_delta_rule_update_split_kernel(
+    A_log,
+    a,
+    b,
+    dt_bias,
+    beta,
+    threshold,
+    q,
+    k,
+    v,
+    o,
+    h0,
+    ht,
+    cu_seqlens,
+    ssm_state_indices,
+    num_accepted_tokens,
+    scale,
+    N: tl.int64,  # num of sequences
+    T: tl.int64,  # num of tokens
+    B: tl.constexpr,
+    H: tl.constexpr,
+    HV: tl.constexpr,
+    K: tl.constexpr,
+    V: tl.constexpr,
+    BK: tl.constexpr,
+    BV: tl.constexpr,
+    stride_init_state_token: tl.constexpr,
+    stride_final_state_token: tl.constexpr,
+    stride_indices_seq: tl.constexpr,
+    stride_indices_tok: tl.constexpr,
+    USE_INITIAL_STATE: tl.constexpr,  # whether to use initial state
+    INPLACE_FINAL_STATE: tl.constexpr,  # whether to store final state inplace
+    USE_QK_L2NORM_IN_KERNEL: tl.constexpr,
+    IS_VARLEN: tl.constexpr,
+    IS_CONTINUOUS_BATCHING: tl.constexpr,
+    IS_SPEC_DECODING: tl.constexpr,
+    IS_KDA: tl.constexpr,
+):
+    # Same independent V tiles; adjacent CTAs now visit adjacent heads.
+    pid = tl.program_id(0)
+    i_k = 0
+    i_v = (pid // HV) % tl.cdiv(V, BV)
+    i_nh = (pid // (HV * tl.cdiv(V, BV))) * HV + pid % HV
+    i_n, i_hv = i_nh // HV, i_nh % HV
+    i_h = i_hv // (HV // H)
+    if IS_VARLEN:
+        bos, eos = (
+            tl.load(cu_seqlens + i_n).to(tl.int64),
+            tl.load(cu_seqlens + i_n + 1).to(tl.int64),
+        )
+        all = T
+        T = eos - bos
+    else:
+        bos, eos = i_n * T, i_n * T + T
+        all = B * T
+
+    if T == 0:
+        # no tokens to process for this sequence
+        return
+
+    o_k = i_k * BK + tl.arange(0, BK)
+    o_v = i_v * BV + tl.arange(0, BV)
+
+    p_q = q + (bos * H + i_h) * K + o_k
+    p_k = k + (bos * H + i_h) * K + o_k
+    p_v = v + (bos * HV + i_hv) * V + o_v
+
+    p_A_log = A_log + i_hv
+    if not IS_KDA:
+        p_a = a + bos * HV + i_hv
+        p_dt_bias = dt_bias + i_hv
+    else:
+        p_a = a + (bos * HV + i_hv) * K + o_k
+        p_dt_bias = dt_bias + i_hv * K + o_k
+
+    p_b = b + bos * HV + i_hv
+    p_o = o + ((i_k * all + bos) * HV + i_hv) * V + o_v
+
+    mask_k = o_k < K
+    mask_v = o_v < V
+    mask_h = mask_v[:, None] & mask_k[None, :]
+
+    b_h = tl.zeros([BV, BK], dtype=tl.float32)
+    if USE_INITIAL_STATE:
+        if IS_CONTINUOUS_BATCHING:
+            if IS_SPEC_DECODING:
+                i_t = tl.load(num_accepted_tokens + i_n).to(tl.int64) - 1
+            else:
+                i_t = 0
+            # num_accepted_tokens is asynchronous GPU state. Keep a stale or
+            # zero count from indexing another request's state row.
+            idx_in_row = (i_t >= 0) & (i_t < stride_indices_seq)
+            state_idx = tl.load(
+                ssm_state_indices + i_n * stride_indices_seq + i_t,
+                mask=idx_in_row,
+                other=0,
+            ).to(tl.int64)
+            # Skip if state index is invalid (NULL_BLOCK_ID=0)
+            if state_idx <= 0:
+                return
+            p_h0 = h0 + state_idx * stride_init_state_token
+        else:
+            p_h0 = h0 + bos * HV * V * K
+        p_h0 = p_h0 + i_hv * V * K + o_v[:, None] * K + o_k[None, :]
+        b_h += tl.load(p_h0, mask=mask_h, other=0).to(tl.float32)
+
+    for i_t in range(0, T):
+        b_q = tl.load(p_q, mask=mask_k, other=0).to(tl.float32)
+        b_k = tl.load(p_k, mask=mask_k, other=0).to(tl.float32)
+        b_v = tl.load(p_v, mask=mask_v, other=0).to(tl.float32)
+        b_b = tl.load(p_b).to(tl.float32)
+
+        # If the model is loaded in fp16, without the .float() here, A might be -inf
+        x = tl.load(p_a).to(tl.float32) + tl.load(p_dt_bias).to(tl.float32)
+        softplus_x = tl.where(
+            beta * x <= threshold, (1 / beta) * tl.log(1 + tl.exp(beta * x)), x
+        )
+        b_g = -tl.exp(tl.load(p_A_log).to(tl.float32)) * softplus_x
+
+        # compute beta_output = sigmoid(b)
+        b_beta = tl.sigmoid(b_b.to(tl.float32))
+
+        if USE_QK_L2NORM_IN_KERNEL:
+            b_q = b_q * (tl.rsqrt(tl.sum(b_q * b_q) + 1e-6))
+            b_k = b_k * (tl.rsqrt(tl.sum(b_k * b_k) + 1e-6))
+        b_q = b_q * scale
+        # [BV, BK]
+        if not IS_KDA:
+            b_h *= tl.exp(b_g)
+        else:
+            b_h *= tl.exp(b_g[None, :])
+        # [BV]
+        b_v -= tl.sum(b_h * b_k[None, :], 1)
+        b_v *= b_beta
+        # [BV, BK]
+        b_h += b_v[:, None] * b_k[None, :]
+        # [BV]
+        b_o = tl.sum(b_h * b_q[None, :], 1)
+        tl.store(p_o, b_o.to(p_o.dtype.element_ty), mask=mask_v)
+
+        # keep the states for multi-query tokens
+        if INPLACE_FINAL_STATE:
+            # Load state index and check for invalid entries
+            final_state_idx = tl.load(
+                ssm_state_indices + i_n * stride_indices_seq + i_t
+            ).to(tl.int64)
+            # Only store if state index is valid (not NULL_BLOCK_ID=0)
+            if final_state_idx > 0:
+                p_ht = ht + final_state_idx * stride_final_state_token
+                p_ht = p_ht + i_hv * V * K + o_v[:, None] * K + o_k[None, :]
+                tl.store(p_ht, b_h.to(p_ht.dtype.element_ty), mask=mask_h)
+        else:
+            p_ht = ht + (bos + i_t) * stride_final_state_token
+            p_ht = p_ht + i_hv * V * K + o_v[:, None] * K + o_k[None, :]
+            tl.store(p_ht, b_h.to(p_ht.dtype.element_ty), mask=mask_h)
+
+        # Update pointers for next timestep
+        p_q += H * K
+        p_k += H * K
+        p_o += HV * V
+        p_v += HV * V
+        p_b += HV
+        p_a += HV
+
+
 def fused_sigmoid_gating_delta_rule_update(
     A_log: torch.Tensor,
     a: torch.Tensor,
@@ -242,8 +423,48 @@
     else:
         stride_indices_seq, stride_indices_tok = ssm_state_indices.stride()
 
+    kernel = fused_sigmoid_gating_delta_rule_update_kernel
     grid = (NK, NV, N * HV)
-    fused_sigmoid_gating_delta_rule_update_kernel[grid](
+    cfg = envs.VLLM_SM12X_GDN_SPEC_CFG
+    if cfg is not None:
+        # Parse even on non-spec calls; mixed prefill/decode keeps its old launch.
+        nominal_bv, tuned_bk, warps, stages, split, variant = parse_spec_config(cfg)
+        if num_accepted_tokens is not None:
+            if (not current_platform.is_device_capability_family(
+                    120, device_id=q.device.index)
+                    or (B, H, HV, K, V) != (1, 8, 24, 128, 128)
+                    or N < 1 or not 0 < T <= N * 10
+                    or cu_seqlens is None or cu_seqlens.ndim != 1
+                    or ssm_state_indices is None
+                    or ssm_state_indices.ndim != 2
+                    or ssm_state_indices.shape[0] < N
+                    or ssm_state_indices.shape[1] != 10
+                    or stride_indices_tok != 1
+                    or num_accepted_tokens.ndim != 1
+                    or num_accepted_tokens.numel() < N
+                    or not inplace_final_state or is_kda
+                    or not use_qk_l2norm_in_kernel
+                    or initial_state.dtype != torch.bfloat16
+                    or not initial_state.is_contiguous()
+                    or tuple(initial_state.shape[1:]) != (24, 128, 128)
+                    or q.shape != k.shape
+                    or tuple(v.shape) != (1, T, 24, 128)
+                    or any(x.dtype != torch.bfloat16 for x in (q, k, v))):
+                raise ValueError("VLLM_SM12X_GDN_SPEC_CFG requires SM12x, "
+                                 "bf16 H=8 HV=24 K=V=128, varlen ns9 metadata, "
+                                 "L2 normalization and contiguous inplace bf16 state")
+            BK, BV = tuned_bk, nominal_bv // split
+            num_warps, num_stages = warps, stages
+            NV = triton.cdiv(V, BV)
+            grid = (1, NV, N * HV)
+            if variant == "split":
+                kernel = fused_sigmoid_gating_delta_rule_update_split_kernel
+                grid = (NV * N * HV,)
+            logger.info_once(
+                "GDN spec update: variant=%s bv=%s bk=%s warps=%s stages=%s "
+                "(HV=24 K=128 V=128 T=10)",
+                variant, BV, BK, num_warps, num_stages, scope="process")
+    kernel[grid](
         A_log=A_log,
         a=a.contiguous(),
         b=b.contiguous(),
--- a/vllm/third_party/flash_linear_attention/ops/gdn_spec_config.py
+++ b/vllm/third_party/flash_linear_attention/ops/gdn_spec_config.py
@@ -0,0 +1,23 @@
+# SPDX-License-Identifier: Apache-2.0
+"""Strict, dependency-free launch configuration for the ns9 GDN update."""
+import functools
+import re
+
+
+@functools.cache
+def parse_spec_config(raw: str) -> tuple[int, int, int, int, int, str]:
+    match = re.fullmatch(
+        r"bv=([0-9]+),bk=([0-9]+),warps=([0-9]+),stages=([0-9]+),split=([0-9]+)"
+        r"(?:,variant=(tiled|split))?", raw)
+    if match is None:
+        raise ValueError("VLLM_SM12X_GDN_SPEC_CFG: expected "
+                         "bv=N,bk=N,warps=N,stages=N,split=N[,variant=tiled|split]")
+    bv, bk, warps, stages, split = map(int, match.groups()[:5])
+    variant = match[6] or "tiled"
+    if (bv not in (8, 16, 32, 64) or bk != 128 or warps not in (1, 2, 4)
+            or stages not in (1, 2, 3) or split not in (1, 2, 4)
+            or bv // split < 8 or (variant == "tiled" and split != 1)):
+        raise ValueError("VLLM_SM12X_GDN_SPEC_CFG: require bv=8/16/32/64, "
+                         "bk=128, warps=1/2/4, stages=1/2/3, split=1/2/4, "
+                         "bv/split>=8; tiled requires split=1")
+    return bv, bk, warps, stages, split, variant
