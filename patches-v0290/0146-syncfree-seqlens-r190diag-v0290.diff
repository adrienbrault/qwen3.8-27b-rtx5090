diff -ruN a/vllm/envs.py b/vllm/envs.py
--- a/vllm/envs.py	2026-09-05 08:07:33.483338238 +0000
+++ b/vllm/envs.py	2026-09-05 08:07:34.828820703 +0000
@@ -11,6 +11,14 @@
 from collections.abc import Callable
 from typing import TYPE_CHECKING, Any, Literal
 
+def _sm12x_seqlens_one_copy() -> bool:
+    name = "VLLM_SM12X_SEQLENS_ONE_COPY"
+    value = os.environ.get(name, "0")
+    if value not in ("0", "1"):
+        raise ValueError(f"{name}: expected exactly 0 or 1, got {value!r}")
+    return value == "1"
+
+
 if TYPE_CHECKING:
     VLLM_HOST_IP: str = ""
     VLLM_PORT: int | None = None
@@ -127,6 +135,7 @@
     VLLM_DISABLED_KERNELS: list[str] = []
     VLLM_USE_HW_AGNOSTIC: bool = False
     VLLM_ENABLE_FLA_PACKED_RECURRENT_DECODE: bool = True
+    VLLM_SM12X_SEQLENS_ONE_COPY: bool = False
     VLLM_SM12X_COLLECTIVE_TAGS: str = "0"
     VLLM_SM12X_GDN_PACKED_BV: int | None = None
     VLLM_GDN_DECODE_KERNEL: Literal["cuda", "triton"] = "cuda"
@@ -1221,6 +1230,7 @@
     "VLLM_ENABLE_FLA_PACKED_RECURRENT_DECODE": lambda: bool(
         int(os.getenv("VLLM_ENABLE_FLA_PACKED_RECURRENT_DECODE", "1"))
     ),
+    "VLLM_SM12X_SEQLENS_ONE_COPY": _sm12x_seqlens_one_copy,
     "VLLM_SM12X_COLLECTIVE_TAGS": env_with_choices(
         "VLLM_SM12X_COLLECTIVE_TAGS", "0", ["0", "1"]
     ),
@@ -2291,6 +2301,9 @@
         "VLLM_DEBUG_DUMP_PATH",
         "VLLM_PORT",
         "VLLM_CACHE_ROOT",
+        # R194 (0146, operator addendum): host-side seq_lens plumbing only, no
+        # traced-graph change; excluded so knob ON/OFF share one compile artifact.
+        "VLLM_SM12X_SEQLENS_ONE_COPY",
         # Runtime memory-plan persistence; does not affect compiled graphs.
         "VLLM_ENABLE_STARTUP_PLAN",
         # Location-only derived paths: where a cache/config directory lives
Binary files a/vllm/__pycache__/envs.cpython-312.pyc and b/vllm/__pycache__/envs.cpython-312.pyc differ
diff -ruN a/vllm/v1/worker/gpu/attn_utils.py b/vllm/v1/worker/gpu/attn_utils.py
--- a/vllm/v1/worker/gpu/attn_utils.py	2026-09-05 08:07:33.725393024 +0000
+++ b/vllm/v1/worker/gpu/attn_utils.py	2026-09-05 08:07:34.820775178 +0000
@@ -3,7 +3,7 @@
 from collections.abc import Mapping, Sequence
 from contextlib import AbstractContextManager, nullcontext
 from dataclasses import dataclass
-from typing import Any, cast
+from typing import TYPE_CHECKING, Any, cast
 
 import torch
 
@@ -31,6 +31,10 @@
 )
 
 
+if TYPE_CHECKING:
+    from vllm.v1.worker.gpu.seqlens_copy import SeqLensSnapshot
+
+
 @dataclass(frozen=True)
 class AttentionCGSupportInfo:
     min_cg_support: AttentionCGSupport = AttentionCGSupport.ALWAYS
@@ -265,6 +269,7 @@
     for_cudagraph_capture: bool = False,
     causal: bool | torch.Tensor | Mapping[int, bool] = True,
     rswa_prefix_lens: torch.Tensor | None = None,
+    sm12x_seq_lens_snapshot: "SeqLensSnapshot | None" = None,
 ) -> dict[str, Any]:
     seq_lens = seq_lens[:num_reqs]
     if dcp_local_seq_lens is not None:
@@ -272,6 +277,32 @@
     if seq_lens_cpu_upper_bound is not None:
         seq_lens_cpu_upper_bound = seq_lens_cpu_upper_bound[:num_reqs]
 
+    shared_cpu_kwargs = {}
+    if sm12x_seq_lens_snapshot is not None:
+        # These builders do not mutate the shared host lengths (DCP excluded).
+        consumers = 0
+        for groups in attn_groups:
+            reads_cpu = False
+            for group in groups:
+                builder = group.get_metadata_builder(0)
+                name = type(builder).__name__
+                if name not in (
+                    "FlashInferMetadataBuilder", "GDNAttentionMetadataBuilder"
+                ) or getattr(builder, "use_dcp", False):
+                    raise ValueError("SM12X one-copy: unsupported metadata builder")
+                reads_cpu |= name == "FlashInferMetadataBuilder"
+            consumers += reads_cpu
+        if for_cudagraph_capture or consumers == 0:
+            raise ValueError("SM12X one-copy: expected eager target metadata build")
+        cpu = sm12x_seq_lens_snapshot.get(consumers)[:num_reqs]
+        shared_cpu_kwargs = {
+            "_seq_lens_cpu": cpu,
+            "_num_computed_tokens_cpu": (
+                cpu - (query_start_loc_cpu[1:num_reqs + 1]
+                       - query_start_loc_cpu[:num_reqs])
+            ),
+        }
+
     attn_metadata: dict[str, Any] = {}
     num_kv_cache_groups = len(kv_cache_config.kv_cache_groups)
     for i in range(num_kv_cache_groups):
@@ -309,6 +340,7 @@
             is_prefilling=group_is_prefilling,
             mm_req_doc_ranges=mm_req_doc_ranges,
             rswa_prefix_lens=rswa_prefix_lens,
+            **shared_cpu_kwargs,
             **common_attn_metadata_extra_kwargs,
         )
 
diff -ruN a/vllm/v1/worker/gpu/input_batch.py b/vllm/v1/worker/gpu/input_batch.py
--- a/vllm/v1/worker/gpu/input_batch.py	2026-09-05 08:07:33.962142558 +0000
+++ b/vllm/v1/worker/gpu/input_batch.py	2026-09-05 08:07:34.820848235 +0000
@@ -11,6 +11,7 @@
 from vllm.utils.math_utils import cdiv
 
 if TYPE_CHECKING:
+    from vllm.v1.worker.gpu.seqlens_copy import SeqLensSnapshot
     from vllm.v1.worker.gpu.block_table import BlockTables
 
 
@@ -111,6 +112,7 @@
     # a query length this batch's own split does not reach, so attention metadata
     # stays valid for every replay the graph serves.
     max_query_len: int | None = None
+    sm12x_seq_lens_snapshot: "SeqLensSnapshot | None" = None
 
     @classmethod
     def make_dummy(
diff -ruN a/vllm/v1/worker/gpu/model_runner.py b/vllm/v1/worker/gpu/model_runner.py
--- a/vllm/v1/worker/gpu/model_runner.py	2026-09-05 08:07:34.196425540 +0000
+++ b/vllm/v1/worker/gpu/model_runner.py	2026-09-05 08:07:34.820926633 +0000
@@ -329,6 +329,24 @@
         self.cudagraph_manager: ModelCudaGraphManager | None = None
 
         # LoRA-related workers.
+        self._sm12x_seq_lens_copy = None
+        if envs.VLLM_SM12X_SEQLENS_ONE_COPY:
+            if (
+                self.speculative_config is None
+                or self.speculative_config.method != "dflash"
+                or self.model_config.hf_config.model_type != "qwen3_5"
+                or self.use_pp or self.use_dcp or self.dp_size != 1
+                or self.parallel_config.prefill_context_parallel_size != 1
+                or self.speculative_config.enable_adaptive_verification
+            ):
+                raise ValueError(
+                    "VLLM_SM12X_SEQLENS_ONE_COPY requires Qwen3.5 DFlash, "
+                    "PP=DP=DCP=PCP=1 and fixed verification"
+                )
+            from vllm.v1.worker.gpu.seqlens_copy import SeqLensCopy
+
+            self._sm12x_seq_lens_copy = SeqLensCopy(self.max_num_reqs, self.device)
+
         self.lora_state = LoraState(max_num_reqs=self.max_num_reqs)
         self.lora_capture_cases = [0]
         if self.lora_config:
@@ -1283,6 +1301,11 @@
             self.input_buffers.seq_lens,
         )
         seq_lens = self.input_buffers.seq_lens[:num_reqs_padded]
+        # Same stream as prepare_pos_seq_lens; before any metadata/device work.
+        seq_lens_snapshot = (
+            self._sm12x_seq_lens_copy.begin(seq_lens)
+            if self._sm12x_seq_lens_copy is not None else None
+        )
 
         dcp_local_seq_lens = None
         if self.use_dcp:
@@ -1348,6 +1371,7 @@
             query_start_loc=query_start_loc,
             query_start_loc_np=query_start_loc_np,
             seq_lens=seq_lens,
+            sm12x_seq_lens_snapshot=seq_lens_snapshot,
             seq_lens_cpu_upper_bound=seq_lens_cpu_upper_bound,
             dcp_local_seq_lens=dcp_local_seq_lens,
             num_computed_tokens_np=num_computed_tokens_np,
diff -ruN a/vllm/v1/worker/gpu/model_states/mamba_hybrid.py b/vllm/v1/worker/gpu/model_states/mamba_hybrid.py
--- a/vllm/v1/worker/gpu/model_states/mamba_hybrid.py	2026-09-05 08:07:34.432350838 +0000
+++ b/vllm/v1/worker/gpu/model_states/mamba_hybrid.py	2026-09-05 08:07:34.821019959 +0000
@@ -316,6 +316,7 @@
             query_start_loc_cpu=query_start_loc_cpu,
             max_query_len=max_query_len,
             seq_lens=input_batch.seq_lens,
+            sm12x_seq_lens_snapshot=input_batch.sm12x_seq_lens_snapshot,
             max_seq_len=max_seq_len,
             block_tables=block_tables,
             slot_mappings=slot_mappings,
diff -ruN a/vllm/v1/worker/gpu/seqlens_copy.py b/vllm/v1/worker/gpu/seqlens_copy.py
--- a/vllm/v1/worker/gpu/seqlens_copy.py	1970-01-01 00:00:00.000000000 +0000
+++ b/vllm/v1/worker/gpu/seqlens_copy.py	2026-09-05 08:07:34.821063471 +0000
@@ -0,0 +1,56 @@
+# SPDX-License-Identifier: Apache-2.0
+"""One exact copy per target batch, NOT shared with post-sampling DFlash."""
+import torch
+
+from vllm.logger import init_logger
+from vllm.utils.gpu_sync_debug import gpu_sync_allowed
+
+logger = init_logger(__name__)
+
+
+class SeqLensSnapshot:
+    def __init__(self, host, source, event):
+        self.host = host
+        self.source = source  # Keep the D2H source alive until completion.
+        self.event = event
+        self.value = None
+
+    def get(self, consumers):
+        if self.value is None:
+            with gpu_sync_allowed():
+                self.event.synchronize()
+            # Detach metadata lifetime from the reusable pinned staging buffer.
+            # Subsequent steps may overwrite staging while old metadata survives.
+            self.value = self.host.clone()
+            self.source = None
+            logger.info_once(
+                "SM12X seq_lens one-copy active: %d target seq_lens_cpu "
+                "consumers share one D2H per step; draft keeps its own copy",
+                consumers,
+            )
+        return self.value
+
+
+class SeqLensCopy:
+    def __init__(self, capacity, device):
+        self.device = device
+        self.host = torch.empty(capacity, dtype=torch.int32, device="cpu",
+                                pin_memory=True)
+        self.pending = None
+
+    def begin(self, source):
+        if torch.cuda.is_current_stream_capturing():
+            raise RuntimeError("SM12X one-copy cannot run inside CUDA capture")
+        if (source.device != self.device or source.dtype != torch.int32
+                or source.ndim != 1 or not source.is_contiguous()
+                or source.numel() > self.host.numel()):
+            raise ValueError("SM12X one-copy: unsupported seq_lens tensor")
+        if self.pending is not None and self.pending.value is None:
+            raise RuntimeError("SM12X one-copy: previous batch was not consumed")
+        host = self.host[:source.numel()]
+        stream = torch.cuda.current_stream(self.device)
+        event = torch.cuda.Event()
+        host.copy_(source, non_blocking=True)
+        event.record(stream)
+        self.pending = SeqLensSnapshot(host, source, event)
+        return self.pending
