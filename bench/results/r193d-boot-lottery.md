# R193d, the per-boot draw is the runtime Triton autotune: two boots on one artifact with `VLLM_TRITON_FORCE_FIRST_CONFIG=1` are bitwise at both contexts (2026-09-05 09:50 to 10:01 UTC, `results/2026-09-05-r193d-boot-lottery`, `scripts/r193d-boot-lottery.sh`)

[← all results](../RESULTS.md)

Two boots, sharded sampling off, with vLLM's `VLLM_TRITON_FORCE_FIRST_CONFIG=1` (every `@triton.autotune` kernel takes its first listed config and skips the benchmark; the knob is a compile factor, so the first boot compiled and the second loaded its artifact). The second boot agrees with the first on 20 of 20 greedy chunks with median and p99 0 at ctx 0 and at 30K. With R193c this closes the question: the compile lottery lives in the inductor reduction configs bundled into the artifact, the per-boot draw in the runtime Triton autotune of the GDN chunked-prefill kernels, and the knob removes the second. Decode is unaffected (steps per second 368 / 370 at 8 streams, 481 / 483 at 16, 74.5 / 74.0 at 1, in the day's band; the decode path runs the packed recurrent kernel, which is not autotuned). Prefill throughput with first-listed configs is not measured here, so the knob is a ruler setting, not a serving one. The first-listed configs are one more draw for the absolute distance to bf16 (0.00818 at 30K, the farthest of the day's 0.0040 to 0.0082 spread).

The protocol for every numerics comparison from here: both engines load one compile artifact (equal hash set, the second boot loads and saves nothing) and both run with `VLLM_TRITON_FORCE_FIRST_CONFIG=1`; a change that does nothing then reads 20 of 20 chunks, median 0, at both contexts, and any divergence is the change. Distances to bf16 across candidates carry both lotteries as noise: about 0.0004 at ctx 0, 0.003 at 30K and 0.035 % dense perplexity per draw. Of today's readings below that size only the divergent ones are void (R191, R193b, R190e, R190c: each could be a draw). A bitwise pair across two modes stands, because two boots read 20 of 20 only if the draw coincided and the change did nothing: R193's sharded-on against sharded-off, R193c's, and R194's knob-on against knob-off are three valid nulls, so batch-sharded sampling is numerically inert on two independent pairs and patch 0146's correctness holds. The pin itself rests on one pair here; R193e below makes it three boots. Three earlier same-artifact pairs were bitwise at 30K and the six R193c pairs were not, which no single account explains yet.
